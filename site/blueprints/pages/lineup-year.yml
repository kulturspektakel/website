title: Lineup

status:
  draft: Entwurf
  unlisted: Öffentlich

columns:
  pages:
    width: 1/1
    sections:
      Bands:
        type: pages
        info: "{{ page.day }} {{ page.time }}, {{ page.stage }}"
        template:
          - band
        create: lineup-year
